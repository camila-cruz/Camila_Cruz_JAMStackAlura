// import { githubService } from '.';

describe('githubService', () => {
  describe('when we need all repos', () => {
    test('all repos are passed', async () => {
      // const githubServiceResponse = await githubService
      //   .getReposFromUser();

      expect(true).toBe(true);
    });
  });

  describe('when a specific repo is requested', () => {
    test('it sends information only about this repo', async () => {
      // const githubServiceResponse = await githubService
      //   .getRepoByName('calculadora');

      expect(true).toBe(true);
    });
  });
});
